version: 1
frontend:
  phases:
    build:
      commands:
        - npx nx build kcf --configuration=production --skip-nx-cache
  artifacts:
    baseDirectory: apps/kcf/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .nx/cache/**/*
